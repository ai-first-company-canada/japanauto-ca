# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.*
# wrangler local dev secrets (JWT_SECRET etc.) — NOT matched by .env.*
.dev.vars
.dev.vars.*

# wrangler local state
.wrangler/

# wrangler `pages functions build` upload artifact
_worker.bundle

# Claude Code per-user settings
.claude/settings.local.json

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# obsidian source-of-truth symlink (cloud-design mockups, ADRs, etc.)
_archives
.social-factory-token.local
.meta-feed-key.local
.reports-unsub-secret.local
