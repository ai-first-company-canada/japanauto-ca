# SEO, SSG & launch gating
> Captured 2026-06-11 from a code-level read of the repository. Per [DOCS-CONVENTIONS.md](../../DOCS-CONVENTIONS.md) R5, verify cited symbols against the code when relying on this document.

## Purpose

The build-time SEO/GEO surface for japanauto.ca: 902 city-first Astro SSG pages plus D1-backed dynamic detail/sitemap Pages Functions, every indexable page carrying title/description/self-canonical/single-H1/Open Graph/JSON-LD, enforced by a Python audit that gates deploy and a stricter LAUNCH mode that refuses to ship fabricated demo inventory or a host-blocking robots.txt before the production domain is connected.

## Key files

| Path | Role |
|---|---|
| `scripts/seo-audit.py` | Pre-deploy SEO/GEO gate. Walks dist/*.html, classifies pages (classify()), computes own_url(), and hard-fails on any indexable page missing title/desc/canonical, with canonical!=own, h1!=1, missing OG, or no/invalid JSON-LD. LAUNCH=1 adds demo-content + robots.txt + sitemap gates. Exit 1 blocks deploy. |
| `src/layouts/BaseLayout.astro` | The SSG head/shell. Emits title (with >65-char brand-suffix trim), meta description, self-canonical, hreflang en-ca + x-default, Open Graph/Twitter, and the root JSON-LD @graph (Organization #organization + WebSite #website) into which page-level schemaLD is spread under @graph. Prop is schemaLD (object|object[]). |
| `src/lib/schema-graph.ts` | Schema.org @graph helper library for SSG content pages: breadcrumbList(), faqPage() (omits placeholder answers), itemList(), authorBio() (slug+display-name lookup w/ editorial fallback), faqFromBody() (lifts ### answers from markdown body into FAQPage + accordion), clampMeta(), blog category derivation/labels. |
| `src/pages/[city]/[make]/[model]/index.astro` | Representative SSG template (~486 of the 902 pages). getStaticPaths() = cities x makes x models. Demonstrates audit-finding #6: Vehicle/Offer and featured-Offer JSON-LD plus the data-demo-content 'Sample preview' banner are all gated on catalog.isDemo so fabricated inventory never gets structured-data markup. |
| `src/data/catalog-stubs.ts` | Pseudo-random stub catalog (getCatalogForModelCity) returning isDemo:true. The isDemo flag is the single source of truth that (a) gates Vehicle/Offer JSON-LD, (b) renders the data-demo-content banner, (c) trips the LAUNCH audit. Must flip to false / per-row when wired to D1. |
| `src/pages/sitemap-static.xml.ts` | Build-time prerendered sitemap (prerender=true) enumerating ~887 static URLs from TIER_1_CITIES x MODELS_BY_BRAND plus brand/blog/glossary content collections, with per-type changefreq/priority. |
| `src/pages/sitemap.xml.ts` | Sitemap index pointing at sitemap-static.xml + the two D1-backed dynamic sitemaps (listings, donors). |
| `functions/sitemap-listings.xml.ts` | Runtime Pages Function: D1 query for active, non-expired listings -> <urlset>, 1h edge cache. Donor sibling at functions/sitemap-donors.xml.ts. |
| `functions/used-cars/listing/[slug].ts` | Dynamic listing detail Pages Function — the honest (non-demo) counterpart: real D1 row -> Vehicle+Offer+BreadcrumbList+FAQPage JSON-LD via renderShell. 404s when row missing / status!='active' / expired (read-side TTL enforcement, audit #8). |
| `functions/_lib/page-shell.ts` | Shared head/shell for dynamic HTML routes mirroring BaseLayout's metadata: title/desc/canonical/OG/Twitter + Organization+WebSite @graph + page schemaLD. esc()/safeUrl() XSS guards; CSP nonce consumed via takeCspNonce(). |
| `public/robots.txt` | Production robots.txt — ships OPEN (Allow: /, plus explicit Allow for ~20 named AI + search crawlers; Disallow only /dealer /api /auth /dashboard). Sitemap: line points at sitemap.xml. Copied verbatim to dist/. |
| `functions/_middleware.ts` | Runtime SEO guard: serves a Disallow:/ robots.txt + X-Robots-Tag noindex ONLY for *.pages.dev preview hosts (keeps staging out of indexes); JWT-gates /dealer/*; builds the CSP header that allowlists the JSON-LD/inline scripts. |
| `.github/workflows/deploy.yml` | CI deploy: typecheck -> build -> 'npm run audit:seo' (audit #19 — replicates the predeploy hook that wrangler-action would otherwise bypass) -> wrangler pages deploy. Note: CI runs audit:seo (normal), not audit:launch. |
| `scripts/generate-csp-hashes.mjs` | Build step that SHA-256s build-time inline scripts for the CSP allowlist; explicitly skips non-executable types (ld+json), so JSON-LD blocks are never hashed and the JSON-LD <script> is unaffected by CSP. Fails build on any on*= handler. |
| `LAUNCH-CHECKLIST.md` | The human launch runbook the LAUNCH audit backstops: replace demo catalog with D1, clear demo dealers/test accounts, then connect japanauto.ca (= instant indexing). The 🤖 items are the ones audit:launch enforces. |
| `package.json` | Defines the gate wiring: audit:seo / audit:launch scripts and the predeploy hook (typecheck -> build -> audit:seo) that fronts 'deploy'. |

## How it works

SSG generation: astro.config.mjs sets site=https://japanauto.ca, build.inlineStylesheets='always', and a remark-strip-faq plugin. Page templates under src/pages/** use getStaticPaths() (e.g. [city]/[make]/[model]/index.astro iterates TIER_1_CITIES x Object.keys(MODELS_BY_BRAND) x models) to emit 902 static HTML files. Every page renders BaseLayout with title/description/canonical (always the literal https://japanauto.ca/<path>/ — city baked in, no client fetch, no data-geo) and an optional schemaLD array built from src/lib/schema-graph.ts helpers (breadcrumbList/itemList/faqPage) or inline literals.\n\nJSON-LD: BaseLayout.astro always emits a root @graph with Organization(#organization, knowsAbout entity list, contactPoint) + WebSite(#website, inLanguage en-CA). A SearchAction was deliberately removed (no /search endpoint — same anti-pattern faqPage() guards). Page schemaLD is spread into @graph (lines 29-74). The JSON-LD <script> escapes '<' to \\u003c. Dynamic routes replicate this via functions/_lib/page-shell.ts renderShell() (a leaner Organization+WebSite graph).\n\nDemo gating (audit #6): catalog-stubs.ts getCatalogForModelCity() returns isDemo:true. In [city]/[make]/[model]/index.astro the Vehicle/Offer ItemList (lines 79-103) and the featured Offer (114-121) are spread only when !catalog.isDemo; when isDemo the page renders a data-demo-content='true' 'Sample preview' banner instead (172-182). So structured data for nonexistent offers is never emitted, and the marker survives into HTML for the launch gate.\n\nNoindex surfaces: AuthLayout.astro and DealerLayout.astro emit <meta name=robots content='noindex, nofollow'>; the audit treats those plus 404.html as non-indexable and skips hard checks (seo-audit.py main() idx flag, line 153).\n\nThe gate: predeploy (package.json) runs typecheck -> build (astro build + extract-css + generate-csp-hashes) -> audit:seo. seo-audit.py scan() regex-extracts title/desc/canonical/robots/h1-count/OG-presence/JSON-LD @types/word-count/data-demo-content per file; main() hard-errors per indexable page on the six invariants and prints a per-group coverage table. LAUNCH=1 (audit:launch) additionally errors if any page carries data-demo-content (capped at 5 listed + a '+N more' line), if dist/robots.txt is missing or matches ^Disallow:\\s*/\\s*$, or if dist/sitemap.xml is absent.\n\nCrawl/index posture: production public/robots.txt ships fully OPEN; the only thing keeping *.pages.dev staging out of indexes is functions/_middleware.ts returning a Disallow:/ robots + X-Robots-Tag noindex for preview hostnames. Connecting japanauto.ca therefore = instant indexing (per memory + LAUNCH-CHECKLIST), which is exactly why the LAUNCH gate exists. Sitemaps: /sitemap.xml (index) -> sitemap-static.xml (build-time, ~887 URLs) + sitemap-listings.xml + sitemap-donors.xml (Pages Functions, D1, 1h cache, currently empty urlsets while D1 is empty — correct).

## Invariants

- Every indexable page (not noindex, not 404) must have: a <title>, a meta description, a <link rel=canonical> whose href === the page's own production URL (https://japanauto.ca/<path>/), exactly one <h1>, all four og: tags (title/description/image/url), and at least one valid JSON-LD block. seo-audit.py hard-fails otherwise and the predeploy/CI gate blocks the deploy.
- Canonical, og:url, and the JSON-LD breadcrumb leaf must all be the city-first literal URL — city is baked into HTML at SSG time. No data-geo* attributes, no client-side geo fetch, no edge HTML rewriting (the audit only sees the literal built HTML).
- Vehicle/Offer (and featured Offer) JSON-LD must NOT be emitted while listings are demo stubs (catalog.isDemo). Marking up nonexistent offers is a Google manual-action risk; the gate is !catalog.isDemo in [city]/[make]/[model]/index.astro.
- FAQPage JSON-LD is only emitted for Q&A pairs that have a real, visible answer — faqPage()/faqFromBody() drop placeholder/empty answers, and the audit's faq_visible-without-FAQPage finding is a WARNING (legitimate when answers are 'coming soon'), never a hard error.
- Demo/fabricated content must be machine-detectable: any stubbed inventory page must stamp data-demo-content into HTML so LAUNCH mode can refuse to ship it.
- The production domain must not be connected until LAUNCH mode is green — connecting japanauto.ca opens indexing immediately, and public/robots.txt is already open. The Disallow:/ protection exists only at runtime for *.pages.dev, not in the shipped robots.txt.
- BaseLayout/page-shell own the Organization + WebSite root @graph; page templates must never re-emit Organization/WebSite (they only supply page-specific nodes via schemaLD).
- sitemap-listings/donors must only list status='active', non-expired rows (read-side TTL enforcement; expire-sweeper is best-effort).
- Declared structured-data actions must be backed by a live endpoint — SearchAction was removed because /search doesn't exist; re-add only when it ships.

## Design decisions

- **Validate SEO/GEO against the built dist/ HTML with a standalone Python regex scanner, run as a predeploy hook and a separate CI step, that hard-fails the deploy.** — The audit must check the exact bytes crawlers and answer engines receive, after Astro rendering, not the source templates. Making it a deploy gate means SEO-broken HTML cannot ship. CI re-runs it because wrangler-action calls 'pages deploy' directly and bypasses the npm predeploy lifecycle hook (audit #19). *Rejected:* An Astro integration / unit tests on component output (rejected — wouldn't see final merged HTML, JSON-LD escaping, or inline-style/marker output); a third-party SEO crawler in CI (heavier, network-dependent, no deploy-blocking semantics).
- **Gate Vehicle/Offer/featured-Offer JSON-LD and the demo banner on a single isDemo boolean rather than emitting markup for stub listings (audit #6).** — Stub catalog rows have URLs that don't resolve and prices that are fabricated; emitting Offer/Vehicle markup for them is structured-data-for-nonexistent-offers (Google manual-action territory). One flag keeps the visible 'Sample preview' banner, the data-demo-content marker, and the schema suppression in lockstep, and makes the launch gate trivial. *Rejected:* Always emit Vehicle/Offer and rely on later cleanup (rejected — risks a manual action the moment the site is indexed); per-row honesty flags now (deferred until the D1 query replaces the stub — comment says 'must become false or derived per-row').
- **Ship public/robots.txt fully open and keep staging out of indexes purely at runtime via middleware (Disallow:/ + X-Robots-Tag only for *.pages.dev), backed by a LAUNCH audit.** — There is no manual 'open robots' step at cutover — connecting the domain must Just Work for indexing. The risk (indexing fabricated/demo data) is moved entirely into the LAUNCH gate, which refuses to build while data-demo-content or a blocking dist/robots.txt is present. *Rejected:* Ship a blocking robots.txt and remember to flip it at launch (rejected — error-prone manual step, and a forgotten flip silently blocks the whole site); environment-specific robots files (more moving parts than a single host-sniffing middleware branch).
- **FAQ answers authored as markdown body prose under '## Common questions', stripped from rendered output by remark-strip-faq and lifted into a <details> accordion + FAQPage via faqFromBody().** — Keeps the FAQPage schema text byte-identical to visible accordion text (Google requires markup to mirror visible content) from a single source of truth, while preserving internal links/bold in the accordion via mdToInlineHtml. *Rejected:* Duplicating Q&A in frontmatter and body (drift risk, and the audit/Google penalize mismatched markup); rendering the prose normally and separately hardcoding schema (double-maintenance).
- **FAQPage-without-visible-FAQ, off-range title/description length, and thin (<200 word) content are WARNINGS, not deploy-blocking errors.** — These are editorial judgement calls, not correctness bugs — e.g. a visible FAQ with placeholder answers SHOULD omit FAQPage. Blocking on them would either force bad markup or stall deploys on subjective thresholds. *Rejected:* Hard-failing on length/thin-content (rejected — too brittle for a content site with legitimate short pages); ignoring them entirely (rejected — they're useful drift signals, surfaced as a counted warning summary).

## Security notes

- CI passes the deploy branch via env DEPLOY_BRANCH instead of interpolating ${{ github.ref_name }} into the shell command (audit #37) — a branch named e.g. fix/x$(...) would otherwise be command-injected on the runner that holds CLOUDFLARE_* secrets.
- JSON-LD output escapes '<' to < in both BaseLayout.astro (set:html) and page-shell.ts renderShell() to prevent </script> breakout / stored-XSS via schema fields.
- generate-csp-hashes.mjs explicitly skips non-executable script types (ld+json) when hashing and fails the build on any inline on*= handler, so the CSP can drop 'unsafe-inline'; the JSON-LD block is unaffected. The async-font-CSS rel-flip script is byte-identical across the three layouts so it dedupes to one CSP hash.
- page-shell.ts (dynamic routes) provides esc() for HTML-context output and safeUrl() that whitelists http/https/mailto/tel and rejects javascript:/data:/vbscript: (incl. tab/newline-obfuscated) before placing user/D1 data into href attributes.
- robots.txt explicitly Disallows /dealer /api /auth /dashboard; /dealer/* is additionally noindex,nofollow at the layout level and JWT-gated in middleware — defense in depth so the authenticated portal is never an SEO surface.
- Preview-host guard (middleware) returns X-Robots-Tag noindex + Disallow:/ for *.pages.dev so staging with demo data cannot be indexed even though the shipped robots.txt is open.

## Gaps / TODO

- catalog-stubs.ts getCatalogForModelCity() is still a pseudo-random STUB returning isDemo:true — the ~486 city-model pages (294 demo-content pages in the current dist) carry data-demo-content, so audit:launch currently FAILS by design. Vehicle/Offer JSON-LD and the live ItemList stay suppressed until this is wired to D1 and isDemo flips.
- LAUNCH robots check has a blind spot: the gate inspects dist/robots.txt (which is the already-open production file, so it passes), while the actual staging block lives only in middleware for *.pages.dev. The gate does NOT verify the runtime preview-host noindex behavior.
- CI deploy.yml runs audit:seo (normal mode), NOT audit:launch — the launch-only demo-content/robots gates are only enforced when a human runs 'npm run audit:launch'. Nothing in CI blocks a pages.dev deploy that still contains demo content (acceptable for staging, but the launch gate is not automated on push).
- D1 listings/donors are empty in prod, so sitemap-listings.xml / sitemap-donors.xml emit empty <urlset> (correct-but-empty), and the /dealers/ index plus 13 demo dealers currently 404 (noted in LAUNCH-CHECKLIST, not yet cleaned).
- Per-row honesty flag is a known future refactor: the isDemo comment states it 'must become false (or derived per-row)' once the real D1 query lands — a single boolean is a placeholder for eventual mixed real/empty inventory.
- Single-locale only: hreflang is self-referential en-ca + x-default with a TODO to add fr-ca alternates when French content ships; no French content exists yet.
- SearchAction (sitelinks searchbox) is intentionally absent pending a live /search endpoint — a deliberate skeleton, re-add gated on that endpoint shipping.
- IndexNow host field is hardcoded to japanauto.pages.dev in batch-ping payloads and must be changed to japanauto.ca post-DNS-cutover (Phase 6 open item).
