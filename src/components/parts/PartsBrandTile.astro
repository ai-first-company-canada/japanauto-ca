---
interface Props {
  name: string;
  slug: string;
  count: number;
  href?: string;
}
const { name, slug, count, href } = Astro.props;
const link = href ?? `/parts/${slug}/`;
---
<a
  class="card"
  href={link}
  style="display:flex;flex-direction:column;justify-content:center;align-items:center;gap:4px;padding:18px 8px;text-decoration:none;min-height:80px"
>
  <span style="font-size:16px;font-weight:600;color:var(--color-ink-strong);letter-spacing:-0.005em">{name}</span>
  <span class="t-caption ink-muted" style="font-size:10px;font-weight:500;letter-spacing:0.06em;text-transform:uppercase">
    <span class="num">{count}</span> donors
  </span>
</a>
