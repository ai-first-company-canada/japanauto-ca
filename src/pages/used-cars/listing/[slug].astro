---
/**
 * Phase 2c2b: this Astro page emits ZERO static paths. Real listing detail
 * pages are served by functions/used-cars/listing/[slug].ts (Pages Function),
 * which fetches D1 and renders HTML at request time. Pages Functions take
 * precedence over the static layer when a path matches.
 *
 * The empty getStaticPaths is here so Astro's TS validator and `wrangler pages
 * deploy` don't complain about a route that emits nothing.
 */
export function getStaticPaths() {
  return [];
}
---
