---
/**
 * Phase 2c2b: this Astro page emits ZERO static paths. Real dealer profile
 * pages are served by functions/dealers/[slug].ts (Pages Function), which
 * fetches D1 and renders HTML at request time. Pages Functions take
 * precedence over the static layer when a path matches.
 */
export function getStaticPaths() {
  return [];
}
---
